version: 2

models:
  - name: mdl18a_lin_upd_tmja_final_19
    description: >
      ModÃ¨le de calcul du TMJA final (tmja_final) en appliquant les ratios dÃ©finis dans le champ coment_tmj_f.
      BasÃ© sur la macro [`mcr_update_tmja_final_lin`](/#!/macro/macro.test_otv.mcr_update_tmja_final_lin).
    data_tests:
      - dbt_utils.equal_rowcount:
          arguments:
            compare_model: ref('mdl1_creer_vue_19')
            description: "VÃ©rifier que le nombre de lignes dans ce modÃ¨le est identique Ã  celui du modÃ¨le de base mdl1_creer_vue_19"
    columns:
      - name: id
        description: "Identifiant unique interne OTR gÃ©nÃ©rÃ© automatiquement"
        data_tests:
          - unique
          - not_null
      - name: id_ign
        description: "Identifiant unique IGN"
        data_tests:
          - unique
          - not_null
      - name: tmja
        description: "TMJA non null pour les tronÃ§ons linÃ©arisÃ©s (coment_cpt = 'linearisation')"
        data_tests:
          - not_null:
              arguments:
                where: "coment_cpt = 'linearisation'"
      - name: tmja_final
        description: "TMJA final calculÃ© en appliquant le ratio dÃ©fini dans coment_tmj_f sur le tmja brut"
        data_tests:
          - not_null:
              arguments:
                where: "coment_cpt = 'linearisation'"
      - name: coment_tmj_f
        description: "Calcul sur le tmja (ex : /2 si sens = Direct ou Inverse) pour dÃ©terminer le tmja final validÃ©"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('coment_tmj_verif') }}"
      - name: coment_cpt
        description: "Type de commentaire sur le comptage (linearisation ou estimation)"
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: "{{ var('coment_cpt_verif') }}"
      - name: long_km
        description: "Longueur du tronÃ§on en km"
        data_tests:
          - not_null
          - dbt_utils.expression_is_true:
              arguments:
                expression: " > 0"
                description: "VÃ©rifier que la longueur du tronÃ§on est positive"

  - name: mdl19_lin_upd_veh_km_19
    description: >
      ModÃ¨le de calcul du nombre de vÃ©hicules-kilomÃ¨tres (veh_km) pour les tronÃ§ons linÃ©arisÃ©s.
      BasÃ© sur la macro [`mcr_update_veh_km_lin`](/#!/macro/macro.test_otv.mcr_update_veh_km_lin).
    data_tests:
      - dbt_utils.equal_rowcount:
          arguments:
            compare_model: ref('mdl1_creer_vue_19')
            description: "VÃ©rifier que le nombre de lignes dans ce modÃ¨le est identique Ã  celui du modÃ¨le de base mdl1_creer_vue_19"
    columns:
      - name: id
        description: "Identifiant unique interne OTR gÃ©nÃ©rÃ© automatiquement"
        data_tests:
          - unique
          - not_null
      - name: id_ign
        description: "Identifiant unique IGN"
        data_tests:
          - unique
          - not_null
      - name: veh_km
        description: "Nombre de vÃ©hicules-kilomÃ¨tres calculÃ© (formule : tmja_final * long_km)"
        data_tests:
          - not_null:
              arguments:
                where: "coment_cpt = 'linearisation'"
          - dbt_utils.expression_is_true:
              arguments:
                expression: "= tmja_final * long_km and veh_km > 0"
                where: "coment_cpt = 'linearisation'"
                description: "VÃ©rifier que le nombre de vÃ©hicules-kilomÃ¨tres est correctement calculÃ© (et > 0) Ã  partir du TMJA final et de la longueur du tronÃ§on"

  - name: mdl20_lin_upd_pl_final_pl_km_19
    description: >
      ModÃ¨le de calcul du nombre de poids lourds final (pl_final) et des poids lourds-kilomÃ¨tres (pl_km).
      BasÃ© sur la macro [`mcr_update_pl_final_pl_km_lin`](/#!/macro/macro.test_otv.mcr_update_pl_final_pl_km_lin).
    data_tests:
      - dbt_utils.equal_rowcount:
          arguments:
            compare_model: ref('mdl1_creer_vue_19')
            description: "VÃ©rifier que le nombre de lignes dans ce modÃ¨le est identique Ã  celui du modÃ¨le de base mdl1_creer_vue_19"
    columns:
      - name: id
        description: "Identifiant unique interne OTR gÃ©nÃ©rÃ© automatiquement"
        data_tests:
          - unique
          - not_null
      - name: id_ign
        description: "Identifiant unique IGN"
        data_tests:
          - unique
          - not_null
      - name: pl_final
        description: "Nombre de poids lourds final calculÃ© en appliquant le ratio dÃ©fini dans coment_tmj_f sur le champ pl"
        data_tests:
          - not_null:
              arguments:
                where: "coment_cpt = 'linearisation' and pl is not null"
          - dbt_utils.expression_is_true:
              arguments:
                expression: ">= 0"
                where: "coment_cpt = 'linearisation' and pl is not null"
                description: "VÃ©rifier que pl_final est positif ou nul"
      - name: pl_km
        description: "Nombre de poids lourds-kilomÃ¨tres calculÃ© (formule : pl_final * long_km)"
        data_tests:
          - not_null:
              arguments:
                where: "coment_cpt = 'linearisation' and pl is not null"
          - dbt_utils.expression_is_true:
              arguments:
                expression: "= pl_final::numeric * long_km::numeric"
                where: "coment_cpt = 'linearisation' and pl is not null and pl_final > 0"
                description: "VÃ©rifier que pl_km est correctement calculÃ© Ã  partir de pl_final et de la longueur du tronÃ§on"
      - name: pl
        description: "Nombre de poids lourds brut issu des donnÃ©es de comptage"
  
  - name: mdl21a_lin_upd_cmt_cpt_19
    description: >
      ModÃ¨le de mise Ã  jour du champ commentaire de comptage (coment_cpt).
      Le champ coment_cpt prend la valeur 'linearisation' si src_cpt = 'otv', sinon 'estimation'.
      BasÃ© sur la macro [`mcr_update_coment_cpt_lin`](/#!/macro/macro.test_otv.mcr_update_coment_cpt_lin).
    data_tests:
      - dbt_utils.equal_rowcount:
          arguments:
            compare_model: ref('mdl1_creer_vue_19')
            description: "VÃ©rifier que le nombre de lignes dans ce modÃ¨le est identique Ã  celui du modÃ¨le de base mdl1_creer_vue_19"
    columns:
      - name: id
        description: "Identifiant unique interne OTR gÃ©nÃ©rÃ© automatiquement"
        data_tests:
          - unique
          - not_null
      - name: id_ign
        description: "Identifiant unique IGN"
        data_tests:
          - unique
          - not_null
      - name: coment_cpt
        description: "Commentaire sur la source du comptage : 'linearisation' si src_cpt='otv', sinon 'estimation'"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('coment_cpt_verif') }}"
          - dbt_utils.expression_is_true:
              arguments:
                expression: "= 'linearisation'"
                where: "src_cpt = 'otv'"
                description: "VÃ©rifier que coment_cpt = 'linearisation' quand src_cpt = 'otv'"
          - dbt_utils.expression_is_true:
              arguments:
                expression: "= 'estimation'"
                where: "src_cpt is null"
                description: "VÃ©rifier que coment_cpt = 'estimation' quand src_cpt est null"
      - name: src_cpt
        description: "Source du comptage"
      - name: pl
        description: "Nombre de poids lourds brut issu des donnÃ©es de comptage"
  
  - name: mdl22_lin_upd_cmt_tmj_f_estim_19
    description: >
      ModÃ¨le final de mise Ã  jour du champ `coment_tmj_f` pour l'ensemble des tronÃ§ons.
      Pour les estimations (coment_cpt='estimation' et src_cpt is null), calcule coment_tmj_f selon le sens de circulation.
      Pour les non-estimations, conserve les valeurs existantes.
      C'est le dernier modÃ¨le de la partie linearisation - tous les tests finaux de validation doivent Ãªtre implÃ©mentÃ©s ici.
      BasÃ© sur la macro [`mcr_update_coment_tmj_f_estimation_lin`](/#!/macro/macro.test_otv.mcr_update_coment_tmj_f_estimation_lin).
    data_tests:
      - dbt_utils.equal_rowcount:
          arguments:
            compare_model: ref('mdl1_creer_vue_19')
            description: "VÃ©rifier que le nombre de lignes dans ce modÃ¨le est identique Ã  celui du modÃ¨le de base mdl1_creer_vue_19"
      - dbt_utils.expression_is_true:
          arguments:
            expression: "((id_comptag::text ~ '[0-9]{2}-(A|N|D|B)[0-9]{1,4}.*-[0-9]{1,4}\\+[0-9]{1,4}'::text OR id_comptag::text ~ '[0-9]{2}-(VC|C).*-[0-9]{1,4}\\+[0-9]{1,4}'::text OR id_comptag::text ~ '[0-9]{2}-(PISTE|VC).*--*[0-9]\\.[0-9]{0,4};[0-9]{1,2}\\.[0-9]{0,4}'::text OR id_comptag::text ~ '[0-9]{2}-DBAC.*'::text OR id_comptag::text ~ '[a-zA-Z]*.*-[a-zA-Z]*.*--?[0-9]\\.[0-9]{0,4};[0-9]{1,2}\\.[0-9]{0,4}'::text) AND (length(id_comptag::text) - length(replace(id_comptag::text, '-'::text, ''::text))) >= 2)"
          config:
            where: "id_comptag is not null"
          description: "VÃ©rifier que l'identifiant de comptage respecte les formats OTR"
      - dbt_utils.expression_is_true:
          arguments:
            expression: "CASE WHEN id_comptag IS NULL THEN coment_cpt = 'estimation' WHEN id_comptag IS NOT NULL THEN coment_cpt = 'linearisation' ELSE FALSE END"
            description: "VÃ©rifier la cohÃ©rence entre id_comptag et coment_cpt (null->estimation, non-null->linearisation)"
      - dbt_utils.expression_is_true:
          arguments:
            expression: "CASE WHEN coment_cpt = 'estimation' THEN src_cpt IS NULL WHEN coment_cpt = 'linearisation' THEN src_cpt IS NOT NULL ELSE FALSE END"
            description: "VÃ©rifier la cohÃ©rence entre coment_cpt et src_cpt (estimation->null, linearisation->not null)"
      - dbt_utils.expression_is_true:
          arguments:
            expression: "coment_tmj_f = '/2' OR coment_tmj_f IS NULL"
            where: "coment_cpt = 'estimation' AND src_cpt IS NULL"
            description: "VÃ©rifier que pour les estimations, coment_tmj_f vaut '/2' (sens unique) ou null (double sens)"
      - dbt_utils.expression_is_true:
          arguments:
            expression: "coment_tmj_f = '/2'"
            where: "coment_cpt = 'estimation' AND src_cpt IS NULL AND sens = ANY(ARRAY{{ var('bdtopo_sens_uniq_verif') }})"
            description: "VÃ©rifier que coment_tmj_f = '/2' pour les estimations en sens unique"
      - dbt_utils.expression_is_true:
          arguments:
            expression: "coment_tmj_f IS NULL"
            where: "coment_cpt = 'estimation' AND src_cpt IS NULL AND sens = '{{ var('bdtopo_double_sens_verif') }}'"
            description: "VÃ©rifier que coment_tmj_f est null pour les estimations en double sens"
    columns:
      - name: id
        description: "Identifiant unique interne OTR gÃ©nÃ©rÃ© automatiquement"
        data_tests:
          - unique
          - not_null
      - name: id_ign
        description: "Identifiant unique IGN"
        data_tests:
          - unique
          - not_null
      - name: nature
        description: "Nature de la route selon IGN"
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: "{{ var('toute_nature_verif') }}"
      - name: sens
        description: "Sens de circulation du tronÃ§on"
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: "{{ var('sens_verif') }}"
                description: "VÃ©rifier que IGN n'a pas changÃ© ses valeurs de *sens*"
      - name: dept
        description: "Code du dÃ©partement sur 2 chiffres"
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: "{{ var('dept_na') }}"
                description: "VÃ©rifier que seules les routes du dÃ©partement concernÃ© -ou de na- sont prÃ©sentes"
      - name: long_km
        description: "Longueur du tronÃ§on en km"
        data_tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0.001
                description: "VÃ©rifier que la longueur est positive"
      - name: numero
        description: "NumÃ©ro de la route, hors voies communales"
        data_tests:
          - dbt_expectations.expect_column_values_to_match_regex:
              arguments:
                regex: "(A|D|N|P)[0-9]+[A-Z]*[0-9]*"
                row_condition: "numero is not null"
                description: "VÃ©rifier que les numÃ©ros respectent un format IGN"
      - name: importance
        description: "Classement IGN de hiÃ©rarchie de rÃ©seau"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('importance_verif') }}"
      - name: cl_admin
        description: "Classement administratif IGN (Autoroute, Nationale, DÃ©partementale)"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('cl_admin_verif') }}"
              config:
                where: "cl_admin is not null"
      - name: gestion
        description: "Gestionnaire selon l'IGN"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('gestion_verif') }}"
              config:
                where: "gestion is not null"
      - name: fictif
        description: "Existence rÃ©elle ou non de la voie selon l'IGN"
        data_tests:
          - not_null
      - name: largeur
        description: "Largeur de la voie en mÃ¨tres"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 25
                description: "VÃ©rifier que la largeur est positive et infÃ©rieure Ã  25m"
              config:
                where: "largeur is not null"
      - name: nb_voies
        description: "Nombre de voies de circulation selon l'IGN"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 10
                description: "VÃ©rifier que le nombre de voies est positif et infÃ©rieur Ã  10"
              config:
                where: "nb_voies is not null"
      - name: etat
        description: "Ã‰tat de la route (projet, construction, en service) selon l'IGN"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('etat_verif') }}"
              config:
                where: "etat is not null"
      - name: coment_cpt
        description: "Commentaire sur la source du comptage : 'linearisation' ou 'estimation'"
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: "{{ var('coment_cpt_verif') }}"
      - name: src_cpt
        description: "Source du comptage : 'otv' ou 'lin_bm' pour les linearisations, null pour les estimations"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('src_cpt_verif') }}"
              config:
                where: "src_cpt is not null"
      - name: id_comptag
        description: "Identifiant du comptage"
        data_tests:
          - dbt_utils.expression_is_true:
              arguments:
                expression: "is not null"
              config:
                where: "coment_cpt = 'linearisation'"
              description: "VÃ©rifier que id_comptag est renseignÃ© pour les linearisations"
          - dbt_utils.expression_is_true:
              arguments:
                expression: "is null"
              config:
                where: "coment_cpt = 'estimation'"
              description: "VÃ©rifier que id_comptag est null pour les estimations"
      - name: src_cpteur
        description: "Source de la donnÃ©e de comptage"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('src_cpteur_verif') }}"
              config:
                where: "src_cpteur is not null"
      - name: ann_pt
        description: "AnnÃ©e du TMJA"
        data_tests:
          - dbt_utils.expression_is_true:
              arguments:
                expression: "~ '^[0-9]{4}$' AND ann_pt::integer >= 2000 AND ann_pt::integer <= {{ var('annee') }}"
              config:
                where: "ann_pt is not null"
              description: "VÃ©rifier que l'annÃ©e du TMJA est au format 4 chiffres et comprise entre 2000 et l'annÃ©e en cours"
      - name: ann_pc_pl
        description: "AnnÃ©e du pourcentage poids-lourds"
        data_tests:
          - dbt_utils.expression_is_true:
              arguments:
                expression: "~ '^[0-9]{4}$' AND ann_pc_pl::integer >= 2000 AND ann_pc_pl::integer <= {{ var('annee') }}"
              config:
                where: "ann_pc_pl is not null"
              description: "VÃ©rifier que l'annÃ©e du %PL est au format 4 chiffres et comprise entre 2000 et l'annÃ©e en cours"
      - name: coment_tmj
        description: "Calcul sur le TMJA pour dÃ©terminer le TMJA final (repris du millÃ©sime N-1)"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('coment_tmj_verif') }}"
              config:
                where: "coment_tmj is not null"
      - name: coment_tmj_f
        description: "Calcul sur le TMJA pour dÃ©terminer le TMJA final (validÃ© pour le millÃ©sime en cours)"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('coment_tmj_verif') }}"
              config:
                where: "coment_tmj_f is not null"
      - name: tmja
        description: "Trafic moyen journalier annuel tous vÃ©hicules"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 300000
                description: "VÃ©rifier que le TMJA est positif et infÃ©rieur Ã  300000"
              config:
                where: "tmja is not null"
      - name: pc_pl
        description: "Part des poids-lourds (en %)"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 100
                description: "VÃ©rifier que le %PL est compris entre 0 et 100%"
              config:
                where: "pc_pl is not null"
      - name: pl
        description: "Trafic moyen journalier annuel poids-lourds"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 150000
                description: "VÃ©rifier que le trafic PL est positif et infÃ©rieur Ã  150000"
              config:
                where: "pl is not null"
      - name: tmja_cpt1
        description: "TMJA du compteur 1"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 150000
                description: "VÃ©rifier que le TMJA cpt1 est positif et infÃ©rieur Ã  150000"
              config:
                where: "tmja_cpt1 is not null"
      - name: tmja_cpt2
        description: "TMJA du compteur 2"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 150000
                description: "VÃ©rifier que le TMJA cpt2 est positif et infÃ©rieur Ã  150000"
              config:
                where: "tmja_cpt2 is not null"
      - name: milieu
        description: "Milieu du tronÃ§on (urbain/rural)"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('milieu_verif') }}"
              config:
                where: "milieu is not null"
      - name: obs_tmja
        description: "MÃ©thode d'affectation du TMJA (auto/mano)"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('obs_tmja_pc_pl_verif') }}"
              config:
                where: "obs_tmja is not null"
      - name: obs_pc_pl
        description: "MÃ©thode d'affectation du %PL (auto/mano)"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('obs_tmja_pc_pl_verif') }}"
              config:
                where: "obs_pc_pl is not null"
      - name: vts_vl_vdf
        description: "Vitesse VL estimÃ©e (VDF)"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 20
                max_value: 140
                description: "VÃ©rifier que la vitesse VL estimÃ©e est comprise entre 20 et 140 km/h"
              config:
                where: "vts_vl_vdf is not null"
      - name: vts_pl_vdf
        description: "Vitesse PL estimÃ©e (VDF)"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 20
                max_value: 90
                description: "VÃ©rifier que la vitesse PL estimÃ©e est comprise entre 20 et 90 km/h"
              config:
                where: "vts_pl_vdf is not null"
      - name: vts_gest
        description: "Vitesse fournie par le gestionnaire"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 140
                description: "VÃ©rifier que la vitesse gestionnaire est comprise entre 0 et 140 km/h"
              config:
                where: "vts_gest is not null"
      - name: vts_osm
        description: "Vitesse rÃ©glementaire OSM"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('vts_osm_verif') }}"
              config:
                where: "vts_osm is not null"
      - name: vts_modif
        description: "Source de correction de vitesse"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('vts_modif_verif') }}"
              config:
                where: "vts_modif is not null"
      - name: vts_vl_f
        description: "Vitesse VL finale corrigÃ©e"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 20
                max_value: 140
                description: "VÃ©rifier que la vitesse VL finale est comprise entre 20 et 140 km/h"
              config:
                where: "vts_vl_f is not null"
      - name: vts_pl_f
        description: "Vitesse PL finale corrigÃ©e"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 10
                max_value: 110
                description: "VÃ©rifier que la vitesse PL finale est comprise entre 10 et 110 km/h"
              config:
                where: "vts_pl_f is not null"
      - name: vts_type_vl
        description: "Type de vitesse VL"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('vts_type_verif') }}"
              config:
                where: "vts_type_vl is not null"
      - name: vts_type_pl
        description: "Type de vitesse PL"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('vts_type_verif') }}"
              config:
                where: "vts_type_pl is not null"
      - name: src_vma
        description: "Source de la vitesse maximale autorisÃ©e"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('src_vma_verif') }}"
              config:
                where: "src_vma is not null"
      - name: vma_vl
        description: "Vitesse maximale autorisÃ©e VL"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('vma_vl_verif') }}"
              config:
                where: "vma_vl is not null"
      - name: vma_pl
        description: "Vitesse maximale autorisÃ©e PL"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('vma_pl_verif') }}"
              config:
                where: "vma_pl is not null"
      - name: vma_type
        description: "Type de vitesse maximale autorisÃ©e"
        data_tests:
          - accepted_values:
              arguments:
                values: "{{ var('vma_type_verif') }}"
              config:
                where: "vma_type is not null"
            # Colonnes dÃ©jÃ  testÃ©es dans d'autres modÃ¨les qu'on doit ajouter ici
      - name: id_simpli
        description: "Identifiant de simplification topologique des tronÃ§ons"
      - name: nom_coll_g
        description: "Nom de rue BdTopo cÃ´tÃ© gauche, issu de systÃ¨me collaboratif"
      - name: nom_coll_d
        description: "Nom de rue BdTopo cÃ´tÃ© droit, issu de systÃ¨me collaboratif"
      - name: inseecom_g
        description: "Code INSEE de la commune cÃ´tÃ© gauche"
      - name: inseecom_d
        description: "Code INSEE de la commune cÃ´tÃ© droit"
      - name: id_voie_g
        description: "Identifiant IGN de la voie cÃ´tÃ© gauche"
      - name: id_voie_d
        description: "Identifiant IGN de la voie cÃ´tÃ© droit"
      - name: urbain
        description: "Si la route est en zone urbaine (1) ou non (0), selon l'IGN"
      - name: vit_moy_vl
        description: "Vitesse moyenne pratiquÃ©e par les vÃ©hicules lÃ©gers selon l'IGN"
      - name: restr_p
        description: "Restriction de poids lourds (en Tonnes) selon l'IGN"
      - name: dept_2024
        description: "DÃ©partement d'appartenance du millÃ©sime 2024 (notion d'appartenance unique pour voies limitrophes)"
      - name: dept_2023
        description: "DÃ©partement d'appartenance du millÃ©sime 2023 (notion d'appartenance unique pour voies limitrophes)"
      - name: veh_km
        description: "Nombre de vÃ©hicules-kilomÃ¨tres (formule : tmja_final * long_km)"
        data_tests:
          - dbt_utils.expression_is_true:
              arguments:
                expression: "= tmja_final * long_km"
              config:
                where: "coment_cpt = 'linearisation' and tmja_final is not null and long_km is not null and veh_km is not null"
              description: "VÃ©rifier que veh_km est correctement calculÃ©"
      - name: tmja_final
        description: "TMJA final sur le tronÃ§on utilisÃ© pour le calcul des vÃ©hicules-kilomÃ¨tres (correspond au trafic du brin SIG)"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 1
                max_value: 150000
                description: "VÃ©rifier que le TMJA final est positif et infÃ©rieur Ã  150000"
              config:
                where: "tmja_final is not null"
      - name: pl_final
        description: "TMJA poids-lourds final sur le tronÃ§on utilisÃ© pour le calcul des PL-kilomÃ¨tres (correspond au trafic PL du brin SIG)"
        data_tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 75000
                description: "VÃ©rifier que le PL final est positif et infÃ©rieur Ã  75000"
              config:
                where: "pl_final is not null"
      - name: pl_km
        description: "Nombre de PL-kilomÃ¨tres (formule : pl_final * long_km)"
        data_tests:
          - not_null:
              arguments:
                where: "coment_cpt = 'linearisation' and pl is not null"
          - dbt_utils.expression_is_true:
              arguments:
                expression: "= pl_final::numeric * long_km::numeric"
              config:
                where: "coment_cpt = 'linearisation' and pl_final is not null and long_km is not null and pl_km is not null and pl_final > 0"
              description: "VÃ©rifier que pl_km est correctement calculÃ©"
      - name: id_cpt1
        description: "Identifiant d'origine du point de comptage issu de la donnÃ©e fournie par le gestionnaire"
      - name: id_cpt2
        description: "Identifiant d'origine du point de comptage issu de la donnÃ©e fournie par le gestionnaire (complÃ©tÃ© si autre compteur prÃ©sent sur la mÃªme section et gÃ©omÃ©trie non dissociÃ©e)"
      - name: obs_tmj1
        description: "Calcul sur le TMJA pour dÃ©terminer le TMJA final liÃ© aux compteurs (si complÃ©tÃ©, champ coment_tmj non utilisÃ©)"        
      - name: obs_tmj2
        description: "Calcul sur le TMJA pour dÃ©terminer le TMJA final liÃ© au sens du tronÃ§on (si complÃ©tÃ©, champ coment_tmj non utilisÃ©)"        
      - name: codau_cat
        description: "Aire urbaine traversÃ©e par le tronÃ§on"      
      - name: codau
        description: "ComplÃ©tÃ© si le tronÃ§on est situÃ© dans une aire urbaine dont la catÃ©gorie de la commune dans le zonage des aires urbaines 2010 est 111 ou 112"        
      - name: type_vdf
        description: "Type de voie selon la mÃ©thode dÃ©veloppÃ©e par le Cerema"        
      - name: id_vts
        description: "Identifiant du point de comptage associÃ© Ã  la table des indicateurs de vitesses obtenus par convention"        
      - name: obs_supl
        description: "Observations supplÃ©mentaires"        
      - name: id_sect
        description: "Identifiant d'origine du sectionnement si fourni par le gestionnaire"        
      - name: src_sect
        description: "Source du sectionnement"
      - name: obs_vts
        description: "Source et annÃ©e de la donnÃ©e vitesse fournie par le gestionnaire (v85, vmoyenneâ€¦)"        
      - name: codau_cont
        description: "Correction du codau_cat pour assurer une continuitÃ© de TMJA ou %PL quand une section de mÃªme importance traverse plusieurs aires urbaines avec des valeurs diffÃ©rentes"        
      - name: id_codau_cont
        description: "Identifiant de la correction de continuitÃ© codau"        
      - name: tmja_cont
        description: "Correction du codau_cat pour assurer une continuitÃ© de TMJA quand une section traverse plusieurs aires urbaines avec des valeurs diffÃ©rentes"  
      - name: pc_pl_cont
        description: "Correction du codau_cat pour assurer une continuitÃ© de %PL quand une section traverse plusieurs aires urbaines avec des valeurs diffÃ©rentes"
      - name: source
        description: "Identifiant du point de dÃ©part du tronÃ§on, topologie crÃ©Ã©e sur la BdTopo de la rÃ©gion Nouvelle Aquitaine (utilisÃ© pour opÃ©rations de routage)"    
        data_tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                description : "l'identifiant de topologie doit etre positif ou =0"   
      - name: target
        description: "Identifiant du point d'arrivÃ©e du tronÃ§on, topologie crÃ©Ã©e sur la BdTopo de la rÃ©gion Nouvelle Aquitaine (utilisÃ© pour opÃ©rations de routage)" 
        data_tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                description : "l'identifiant de topologie doit etre positif ou =0"      
      - name: cnt_src
        description: "Nombre de brins reliÃ©s Ã  l'extrÃ©mitÃ© source du tronÃ§on (utilisÃ© pour dÃ©finir des sections homogÃ¨nes de trafic)"
        data_tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 1
                description : "un noeud est toujours au moins reliÃ© Ã  1 brin"
      - name: cnt_tgt
        description: "Nombre de brins reliÃ©s Ã  l'extrÃ©mitÃ© target du tronÃ§on (utilisÃ© pour dÃ©finir des sections homogÃ¨nes de trafic)"
        data_tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 1
                description : "un noeud est toujours au moins reliÃ© Ã  1 brin"
      - name: attr_modif
        description: "Attributs modifiÃ©s (importance, numÃ©ro) lors du changement d'Ã©dition de la BdTopo"       
      - name: geom
        description: "GÃ©omÃ©trie de type ligne 3D dans le systÃ¨me de projection Lambert 93 / EPSG 2154 (MultiLineStringZ, 2154)"
        data_tests:
          - not_null
      - name: autor_sect
        description: "[Ã€ PRÃ‰CISER] Indicateur d'autorisation de sectionnement du tronÃ§on"
      - name: imp_sup1
        description: "[Ã€ PRÃ‰CISER] Importance supÃ©rieure option 1 - critÃ¨re complÃ©mentaire de hiÃ©rarchisation du rÃ©seau"
      - name: imp_sup2
        description: "[Ã€ PRÃ‰CISER] Importance supÃ©rieure option 2 - critÃ¨re complÃ©mentaire de hiÃ©rarchisation du rÃ©seau"
      - name: id_cnt2
        description: "[Ã€ PRÃ‰CISER] Identifiant de comptage alternatif ou secondaire"
      - name: id_struct_rout
        description: "[Ã€ PRÃ‰CISER] Identifiant de la structure routiÃ¨re associÃ©e au tronÃ§on"
      - name: id_sect_hom
        description: "[Ã€ PRÃ‰CISER] Identifiant de section homogÃ¨ne de trafic regroupant plusieurs tronÃ§ons"
      - name: id_bdc
        description: "[Ã€ PRÃ‰CISER] Identifiant BdCarto du tronÃ§on"
      - name: ang_orient_src_vert1
        description: "[Ã€ PRÃ‰CISER] Angle d'orientation entre le noeud source et le premier vertex de la gÃ©omÃ©trie (utilisÃ© pour analyses d'orientation)"
      - name: ang_orient_src_vert2
        description: "[Ã€ PRÃ‰CISER] Angle d'orientation entre le noeud source et le deuxiÃ¨me vertex de la gÃ©omÃ©trie (utilisÃ© pour analyses d'orientation)"
      - name: ang_orient_tgt_vert1
        description: "[Ã€ PRÃ‰CISER] Angle d'orientation entre l'avant-dernier vertex de la gÃ©omÃ©trie et le noeud target (utilisÃ© pour analyses d'orientation)"
      - name: ang_orient_tgt_vert2
        description: "[Ã€ PRÃ‰CISER] Angle d'orientation entre le dernier vertex de la gÃ©omÃ©trie et le noeud target (utilisÃ© pour analyses d'orientation)"
      - name: list_id_inter
        description: "[Ã€ PRÃ‰CISER] Liste des identifiants d'intersections traversÃ©es par le tronÃ§on ou adjacentes"
      - name: nb_nod_non_topo
        description: "[Ã€ PRÃ‰CISER] Nombre de noeuds non topologiques sur le tronÃ§on (vertices intermÃ©diaires sans connexion rÃ©elle)"
      - name: id_struct
        description: "[Ã€ PRÃ‰CISER] Identifiant unique de structure (ponts, tunnels, ouvrages d'art) associÃ©e au tronÃ§on"
